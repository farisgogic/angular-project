RewriteEngine On
RewriteBase /

# Handle Angular routes - redirect all requests to index.html if the file doesn't exist
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
